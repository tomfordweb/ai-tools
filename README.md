Start the app by running:

```bash
docker compose up --build
```

If you would like to work with langchain. Run the following command which will start up a TTY for you to hack on.

```bash
./bin/app.sh
```

If you would like to download a model progmatically:

```bash
./bin/downloadModel.sh <modelName>
```
